# Daily Study Log — 17 August 2026

## 📅 Date
**17 August 2026**

## 🎯 Study Summary

Today was a focused cybersecurity + MSCCS-104 study session. Japanese was **not studied today**.

### 🔐 Cybersecurity — Linux

**Topic:** Linux Process, Network Socket & systemd Investigation

Covered and practiced:

- Process enumeration with `ps aux` and `ps -p`
- Process trees with `pstree -p`
- Investigating running processes through `/proc/<PID>/`
- Mapping a process to its executable using `/proc/<PID>/exe`
- Investigating process file descriptors with `/proc/<PID>/fd`
- Understanding `socket:[inode]` entries
- Connecting socket inodes to `/proc/net/*`
- Distinguishing TCP, TCP6, UDP, UDP6 and Unix sockets
- Using `ss` to correlate listening/network sockets with processes and PIDs
- Linux service enumeration with `systemctl`
- `systemctl status`
- Difference between `systemctl is-active` and `systemctl is-enabled`
- Reading service definitions with `systemctl cat`
- Understanding `ExecStart`
- Service dependencies with `systemctl list-dependencies`
- Service logs with `journalctl -u`
- Locating and investigating custom systemd `.service` files
- Understanding why systemd services can be relevant to persistence investigations

### 🧮 MSCCS-104 — Computational Number Theory / Cryptography

Continued foundational modular arithmetic work.

Practiced:

- Modular congruence and the meaning of `a ≡ b (mod n)`
- Understanding congruence through divisibility/remainders
- Modular inverse
- Recognising when a modular inverse exists
- Using the Extended Euclidean Algorithm for modular inverses
- Worked through small examples to build intuition rather than relying only on memorised tricks

### 🇯🇵 Japanese

**Not studied today.**

## 🧠 Key Learning

The cybersecurity session connected several Linux concepts into one investigation chain:

```text
systemd service
      ↓
unit file / ExecStart
      ↓
process / PID
      ↓
/proc/<PID>
      ↓
file descriptors / sockets
      ↓
network exposure
```

For MSCCS-104, the focus remained on developing reliable intuition for modular arithmetic before moving deeper into number-theoretic algorithms.

## 📌 Next Session

- Continue MSCCS-104 from modular inverses / Extended Euclidean Algorithm.
- Continue Linux security investigation with a new topic rather than repeating basic Text-Fu or permissions.
- Resume Japanese when scheduled.

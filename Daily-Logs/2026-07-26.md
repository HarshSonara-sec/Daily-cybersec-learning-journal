# Daily Log - 26 July 2026

## Networking
- Studied **CCNA Day 12 - Dynamic Routing**.
- Learned the fundamentals of dynamic routing, routing protocols, administrative distance, metrics, RIP, OSPF, EIGRP, and BGP overview.
- Studied the **Life of a Packet**, following a packet's journey from the source host to the destination across multiple routers.
- Reviewed Layer 2 and Layer 3 encapsulation and de-encapsulation at each network hop.
- Understood how routers make forwarding decisions using routing tables while switches forward frames using MAC addresses.

## Linux
- Practiced intermediate Linux commands in the custom `linux-lab`.
- Learned and practiced:
  - `find` for locating files and directories.
  - `grep` for searching text inside files.
  - `less` for viewing and searching large files.
  - `head`, `tail`, and `tail -f` for log analysis and real-time monitoring.
  - Pipes (`|`) for combining commands.
  - Input/output redirection (`>`, `>>`, `2>`, `2>&1`).
  - `tee` for displaying and saving output simultaneously.
  - `sha256sum` for verifying file integrity.
  - Hard links, inodes, and link counts.

## Practical Labs
- Completed Linux command practice exercises.
- Created an incident response report using Linux commands.
- Monitored log files in real time using `tail -f`.
- Practiced troubleshooting and successfully resolved an issue with log monitoring.
- Created and analyzed hard links to understand inode behavior and Linux file management.

## Key Takeaways
- `find` locates files and directories, while `grep` searches inside text.
- Combining commands with pipes and redirection creates powerful command-line workflows.
- `tail -f` is essential for monitoring live log files.
- Hard links are additional directory entries pointing to the same inode rather than copies of a file.
- Understanding the **Life of a Packet** helps connect OSI model concepts with real network communication and router forwarding decisions.

## Next Session
- Review today's Linux concepts.
- Learn `sort`, `uniq`, `cut`, and advanced `wc`.
- Begin Regular Expressions (Regex) and apply them to log analysis.
